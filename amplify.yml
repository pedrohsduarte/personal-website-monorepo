version: 1
applications:
  - appRoot: apps/site
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - yarn global add turbo
            - yarn install
        build:
          commands:
            - yarn build
            - cd apps/site
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - '../../../apps/site/.next/cache/**/*'
          - '../../../node_modules/**/*'
